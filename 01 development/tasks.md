# Tasks

## In progress
- [ ] Set up NotebookLM "the aqua portal finalised" notebook for outside
      research + Q&A. Needs Ed to provide the share URL after first auth.
- [ ] Draft three self-contained prompts for the parallel terminals (Phase 1
      architecture work split three ways).
- [ ] Have Ed populate `eds requirments.md` so it can be cited in the
      terminal prompts.

## Up next
- [ ] **Phase 1**: lock in URL surface + role hierarchy + folder layout for
      `04 the final portal/portal/`. Document in `context/` chapters.
- [ ] **Phase 2**: scaffold `04/portal/` as a fresh Next.js 16 app with
      Aqua's plugin platform copied in (`_types.ts` / `_registry.ts` /
      `_runtime.ts` / `_presets.ts`).
- [ ] **Phase 3**: build the fulfillment slice (create client, pick phase
      preset, install plugins per client).
- [ ] **Phase 4**: port website-editor + ecommerce plugins from `02` into
      `04/plugins/`.

## Done
- [x] Phase 0 — Prior research. 18 chapters in
      `01 development/context/prior research/`. Indexed in `MASTER.md`.
      All commits live on `main`.

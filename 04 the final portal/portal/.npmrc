# Local workspace plugins (`@aqua/plugin-*`) install as `file:..`
# dependencies. Turbopack 16 doesn't follow npm's default symlinks for
# those, so we tell npm to copy the package into `node_modules` instead.
# When iterating on a plugin source, re-run `npm install` to refresh.
install-links=true

// Public component-side surface for `@aqua/plugin-website-editor/components`.
//
// Cross-plugin and foundation consumers reach into this module to render
// editor-owned blocks and storefront overlays. Internal editor widgets
// (canvas, properties panel, etc.) are NOT re-exported — they live behind
// the manifest's `pages[]` entries.
//
// **STUB** — fleshed out in steps 3–5 as components land.

export {};

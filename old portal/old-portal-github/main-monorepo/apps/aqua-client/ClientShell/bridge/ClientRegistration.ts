import { BridgeRegistry } from '@aqua/bridge/registry';

/** ClientRegistration — alias of BridgeRegistry for the Client shell */
export const ClientRegistration = BridgeRegistry;
export { BridgeRegistry };

/**
 * demoComponentMap — Stubbed version
 */

import type { ComponentType } from 'react';

/**
 * Maps view IDs → demo component variants.
 * Stubbed to allow shell boot without templates.
 */
export const demoComponentMap: Record<string, ComponentType<any>> = {};

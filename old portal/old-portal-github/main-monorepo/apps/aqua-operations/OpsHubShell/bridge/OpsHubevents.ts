/**
 * OpsHubShell Bridge Events
 *
 * Thin re-export of the canonical typed BridgeEvents from @aqua/bridge.
 * Single event bus across the entire application.
 */

export { BridgeEvents } from '@aqua/bridge/events';
export type { BridgeEventMap } from '@aqua/bridge/events';

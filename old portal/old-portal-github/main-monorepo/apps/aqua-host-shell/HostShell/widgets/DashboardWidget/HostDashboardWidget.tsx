/**
 * Re-export of the kit-styled DashboardWidget from Bridge.
 * Kept as a per-app file so the widget index path stays stable.
 */
export { DashboardWidget } from '@aqua/bridge/ui/DashboardWidget';
export type { IconName } from '@aqua/bridge/ui/DashboardWidget';
